# ============================================================
#  普通化學(一) — 不上傳 GitHub 的檔案
#  注意：git 不支援「行尾註解」。規則後面不可以接 # 說明，
#        否則整行（含空白與 #）會被當成檔名，規則等於失效。
#        所有說明一律另起一行。
# ============================================================

# ── 教科書 PDF ──────────────────────────────────────────────
# chemistry-2e_-_WEB.pdf 為 208 MB，超過 GitHub 單檔 100 MB 硬上限，
# push 會被直接拒絕；OpenStax 原書可自官網取得，不需入庫。
*.pdf

# ── 含答案，絕不可公開 ──────────────────────────────────────
# 題庫.csv：W1–W5 共 56 題，含答案與誘答診斷
題庫.csv
# 課堂表單：內含 AI 驗算單的 2 頁教師解答卷與計分規準
docs/課堂表單_*.html
# 教師專用入口：連往上述兩者
docs/教師專用.html

# ── 開發用工具 ──────────────────────────────────────────────
# 對學生無意義；要公開就把這兩行刪掉
_圖表產生器/
_build_quiz_template.py

# ── 以下請自行決定是否排除 ──────────────────────────────────
# 這五份是教學設計與計畫書策略文件（教材檢討、申請角度、風險分析）。
# 目前「不」排除 —— repo 若設為 public，它們會連同投影片一起公開。
# 不想公開就把下面六行前面的 # 拿掉：
教材檢討報告_20260814.md
教學實踐研究計畫_申請角度建議.md
學習成效評估_平台與AI融入.md
形成性評量設計.md
食品營養導向重構建議.md
docs/W1_W5_W6_W16_試做交付物.md

# ── L4 課後精熟測驗 ───────────────────────────────────────────────────
# 整個目錄都不能公開：題庫主檔、教師卷、建表單的 .gs 裡都寫著正解與誘答診斷。
# L4 是唯一要留下可分析紀錄的一層，答案一旦外洩，這層就失去意義。
_L4測驗/

# 新課綱 deck 的產生器(投影片本身在 docs/ 是公開的,這裡只是產生它們的腳本)
_新課綱/

# 互動平台的匯入題庫(教師工作檔,含正解;內容來自 L2,學生本來就看得到,
# 但不需要出現在公開網站上)
_平台題庫/

# CLAUDE.md 是給 AI 助理看的工作說明,裡面記錄了 L4 某一題的三個誘答數值
# 與各自對應的錯誤 —— 學生用刪去法就能反推正解,所以不公開。
CLAUDE.md

# 原始 18 週教材(week01–week17 與四個 pilot)不再上線 —— 課程已改用食品營養導向的
# 16 週編號。檔案<b>留在本機</b>,因為 _新課綱/ 的產生器要從它們抽取投影片。
# 計算專題已改名為 docs/新W01b_計算專題.html,不在 week* 樣式裡,自然會上線。
docs/week*.html

# ── 雜項 ────────────────────────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store
__pycache__/
*.pyc
